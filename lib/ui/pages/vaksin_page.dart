import 'package:flutter/material.dart';

class VaksinPage extends StatelessWidget {
  const VaksinPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}