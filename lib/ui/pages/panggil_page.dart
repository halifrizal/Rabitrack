import 'package:flutter/material.dart';

class PanggilPage extends StatelessWidget {
  const PanggilPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}